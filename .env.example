# OpenSearch admin password (required)
OPENSEARCH_ADMIN_PASSWORD=change_me_strong_password

# PostgreSQL password (defaults to "postgres" if not set)
POSTGRES_PASSWORD=postgres

# CORS allowed origins for the REST API (comma-separated; defaults to http://localhost:5173)
CORS_ALLOWED_ORIGINS=http://localhost:5173
