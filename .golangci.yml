# golangci-lint v2 configuration.
# Docs: https://golangci-lint.run/usage/configuration/
version: "2"

run:
  timeout: 5m
  # vendor/ is auto-skipped by golangci-lint; listed for clarity.
  modules-download-mode: readonly

linters:
  # Start with default linters (errcheck, govet, staticcheck, ineffassign,
  # unused — gosimple is folded into staticcheck in v2) and layer security
  # / correctness checks on top.
  default: standard
  enable:
    - bodyclose    # unclosed HTTP response bodies (high-impact for this repo)
    - errorlint    # errors.Is/As + %w wrapping correctness
    - gocritic     # opinionated correctness/perf/style checks
    - gosec        # security analysis (G-rules)
    - nilerr       # returning nil after non-nil err checks
    - noctx        # http requests built without context
    - revive       # broad style/best-practices linter (replaces golint)

  exclusions:
    paths:
      - vendor
      # zefix-tools is mostly legacy/imported code with its own conventions.
      # TODO: tighten this once the package is refactored; for now we keep the
      # default linters running but drop gosec/revive there to avoid drowning
      # the signal with low-priority warnings.
      - zefix-tools/cmd
    rules:
      # Tests routinely ignore errors on cleanup and use unchecked type
      # assertions for table-driven cases. Keep them readable.
      - path: _test\.go
        linters:
          - errcheck
          - gosec
          - bodyclose
          - noctx
      # Targeted carve-out for the legacy zefix-tools tree.
      - path: zefix-tools/
        linters:
          - gosec
          - revive

      # --- Pre-existing lint debt (pre-dates security hardening) ---
      # TODO: dedicate a refactor pass to clear these exclusions.

      # 177 pre-existing revive issues (missing godoc, var-naming, etc.).
      # All pre-date the security hardening pass.
      - linters:
          - revive
        text: ".*"

      # 29 pre-existing errcheck issues on defer .Close() patterns.
      # These are low-signal for HTTP bodies and temp files.
      - linters:
          - errcheck
        text: "Error return value of .*Close.* is not checked"

      # Pre-existing gosec issues in server (G104 from opensearch body close)
      # and zefix (G104), plus CLI tools (G304 from user-supplied file paths).
      # All pre-date the security hardening pass.
      - path: (internal/server/|pkg/zefix/|internal/cli/)
        linters:
          - gosec

issues:
  max-issues-per-linter: 0
  max-same-issues: 0

formatters:
  enable:
    - gofmt
    - goimports
